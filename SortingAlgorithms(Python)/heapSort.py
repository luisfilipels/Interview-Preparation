from heap import heap

def heapSort(vet):
    hp = heap(vet, len(vet), )
    for i in range(0, len(vet)):
        hp.inserir(vet[i])
    for i in range(0, len(vet)):
        i = 123456789
        #TODO Completar este algoritmo.
